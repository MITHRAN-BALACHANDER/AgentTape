name: CI

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  lint:
    name: Lint & type-check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install uv
        uses: astral-sh/setup-uv@v5
        with:
          python-version: "3.12"
      - name: Install
        run: uv pip install --system -e ".[dev,openai,http]"
      - name: Ruff (lint)
        run: ruff check src tests
      - name: Ruff (format)
        run: ruff format --check src tests
      - name: Mypy (strict, core)
        run: mypy

  test:
    name: Test (py${{ matrix.python-version }} · ${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
        python-version: ["3.10", "3.11", "3.12", "3.13"]
    steps:
      - uses: actions/checkout@v4
      - name: Install uv
        uses: astral-sh/setup-uv@v5
        with:
          python-version: ${{ matrix.python-version }}
      - name: Install
        run: uv pip install --system -e ".[dev,openai,http]"
      - name: Run tests with coverage
        run: coverage run --branch --source=agenttape -m pytest -q
      - name: Coverage report
        run: coverage report
      - name: Enforce core-engine coverage threshold
        run: coverage report --include="*/agenttape/engine.py" --fail-under=90

  test-zero-deps:
    name: Test core with zero optional deps (stdlib YAML path)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v5
        with:
          python-version: "3.12"
      - name: Install core + pytest only (no PyYAML, no adapters)
        run: uv pip install --system -e . pytest
      - name: Run core tests forcing the stdlib YAML path
        env:
          AGENTTAPE_FORCE_STDLIB_YAML: "1"
        run: pytest -q tests/test_yaml_io.py tests/test_engine.py tests/test_core_units.py tests/test_freeze.py
